<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#121212">

    <!-- Botón 1: CONTESTAR (Ocupa el 50% superior) -->
    <Button
        android:id="@+id/btnContestar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:text="CONTESTAR"
        android:textSize="32sp"
        android:textStyle="bold"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF" />

    <!-- Botón 2: FINALIZAR / COLGAR (Ocupa el 25% del medio) -->
    <Button
        android:id="@+id/btnColgar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="FINALIZAR"
        android:textSize="26sp"
        android:textStyle="bold"
        android:backgroundTint="#F44336"
        android:textColor="#FFFFFF" />

    <!-- Botón 3: ALTAVOZ (Ocupa el 25% inferior) -->
    <Button
        android:id="@+id/btnAltavoz"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="ALTAVOZ (OFF)"
        android:textSize="26sp"
        android:textStyle="bold"
        android:backgroundTint="#2196F3"
        android:textColor="#FFFFFF" />

</LinearLayout>
